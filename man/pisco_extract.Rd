% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{pisco_extract}
\alias{pisco_extract}
\title{Extract Time Series from PISCO Rasters}
\usage{
pisco_extract(x, points = NULL, polygons = NULL, fun = "mean", id_col = NULL)
}
\arguments{
\item{x}{A \link[terra:SpatRaster]{terra::SpatRaster} object.}

\item{points}{Optional. An \code{sf} point object, a matrix/data.frame with \code{lon} and \code{lat} columns,
or a numeric coordinate vector \code{c(lon, lat)}.}

\item{polygons}{Optional. An \code{sf} polygon object or \code{terra::SpatVector} for zonal statistics.}

\item{fun}{Character or function. Summary statistic when extracting across polygons
(e.g., \code{"mean"}, \code{"sum"}, \code{"median"}). Default is \code{"mean"}.}

\item{id_col}{Character. Name of the column in \code{polygons} or \code{points} to identify features.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} containing the extracted time series in tidy format.
}
\description{
Extracts precipitation time series from a PISCO \link[terra:SpatRaster]{terra::SpatRaster} at given point
coordinates or computes zonal summaries (e.g. areal mean) over spatial polygons.
}
\examples{
\dontrun{
r <- pisco_read("monthly")
# Extract at point coordinates (Cusco)
df_pt <- pisco_extract(r, points = c(-71.96, -13.53))

# Extract areal mean over watershed polygons
# df_basin <- pisco_extract(r, polygons = basins_sf, id_col = "basin_name")
}
}
