% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{pisco_download}
\alias{pisco_download}
\title{Download PISCO Datasets}
\usage{
pisco_download(
  dataset = c("monthly", "daily", "climatology"),
  destdir = pisco_cache_dir(),
  overwrite = FALSE,
  verify_md5 = TRUE,
  timeout = 3600,
  quiet = FALSE
)
}
\arguments{
\item{dataset}{Character. The dataset to download: \code{"monthly"} (\code{"PISCOp_m"}),
\code{"daily"} (\code{"PISCOp_d"}), or \code{"climatology"} (\code{"PISCOp_clim2"}). Default is \code{"monthly"}.}

\item{destdir}{Character. Destination directory. Default is \code{\link[=pisco_cache_dir]{pisco_cache_dir()}}.}

\item{overwrite}{Logical. If \code{TRUE}, re-downloads the file even if present in cache.}

\item{verify_md5}{Logical. If \code{TRUE}, validates the MD5 checksum after download. Default is \code{TRUE}.}

\item{timeout}{Numeric. Maximum seconds to allow for download. Default is 3600 (1 hour),
accommodating large files like \code{PISCOp_d.nc} (~1.52 GB).}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages. Default is \code{FALSE}.}
}
\value{
Character. The absolute path to the downloaded (or cached) file.
}
\description{
Downloads a PISCO dataset from the official Figshare repository to the local
cache directory. If the file is already cached and complete, download is skipped
unless \code{overwrite = TRUE}.
}
\examples{
\dontrun{
# Download monthly precipitation (56.6 MB)
fpath <- pisco_download("monthly")

# Download normal climatology 1991-2015 (1.83 MB)
fpath_clim <- pisco_download("climatology")
}
}
