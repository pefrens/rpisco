% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{pisco_anomaly}
\alias{pisco_anomaly}
\title{Calculate Rainfall Anomalies}
\usage{
pisco_anomaly(
  x,
  baseline = NULL,
  type = c("percentage", "difference"),
  eps = 0.1
)
}
\arguments{
\item{x}{A monthly \link[terra:SpatRaster]{terra::SpatRaster} with valid time attributes.}

\item{baseline}{Optional \link[terra:SpatRaster]{terra::SpatRaster} of monthly normals (12 layers) representing
the climatological baseline (e.g. \code{pisco_read("climatology")}). If \code{NULL}, the baseline
is computed internally as the multi-year monthly mean of \code{x}.}

\item{type}{Character. Type of anomaly: \code{"difference"} (absolute anomaly: \code{x - baseline} in mm)
or \code{"percentage"} (relative anomaly: \code{((x - baseline) / baseline) * 100} in \\\%). Default is \code{"percentage"}.}

\item{eps}{Numeric. Small epsilon to prevent division by zero in arid regions when \code{type = "percentage"}.
Default is 0.1 (mm).}
}
\value{
A \link[terra:SpatRaster]{terra::SpatRaster} of anomalies with preserved time attributes.
}
\description{
Computes precipitation anomalies relative to a baseline climatology (such as \code{PISCOp_clim2}
or a multi-year monthly mean). Anomalies can be expressed in absolute difference (mm)
or percentage deviation (\\\%).
}
\examples{
\dontrun{
r_m <- pisco_read("monthly", dates = c(1997, 1998))
r_clim <- pisco_read("climatology")

# Percentage anomaly during 1997-1998 El Nino
anom_pct <- pisco_anomaly(r_m, baseline = r_clim, type = "percentage")

# Absolute difference anomaly (mm)
anom_diff <- pisco_anomaly(r_m, baseline = r_clim, type = "difference")
}
}
