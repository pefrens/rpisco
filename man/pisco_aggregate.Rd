% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{pisco_aggregate}
\alias{pisco_aggregate}
\title{Temporal Aggregation of PISCO Rasters}
\usage{
pisco_aggregate(
  x,
  by = c("year", "month", "season_senamhi", "hydrological_year", "climatology"),
  fun = "sum"
)
}
\arguments{
\item{x}{A \link[terra:SpatRaster]{terra::SpatRaster} with valid time attributes.}

\item{by}{Character. Aggregation unit:
\itemize{
\item \code{"year"}: Aggregates across each calendar year (e.g., annual rainfall).
\item \code{"month"}: Multi-year monthly aggregation (produces 12 layers for Jan–Dec).
\item \code{"season_senamhi"}: SENAMHI official hydrological seasons:
Wet season (\code{wet_Nov_Apr}: Nov, Dec, Jan, Feb, Mar, Apr) and
Dry season (\code{dry_May_Oct}: May, Jun, Jul, Aug, Sep, Oct).
November and December are grouped with the following calendar year's hydrological period.
\item \code{"hydrological_year"}: Annual aggregation based on the Peruvian hydrological year
starting in November (Nov 1 to Oct 31).
\item \code{"climatology"}: Multi-year monthly mean normals (12 layers).
}}

\item{fun}{Character or function. Aggregation function (\code{"sum"}, \code{"mean"}, \code{"max"}, etc.).
Default is \code{"sum"} (for precipitation accumulations).}
}
\value{
An aggregated \link[terra:SpatRaster]{terra::SpatRaster}.
}
\description{
Aggregates a PISCO \link[terra:SpatRaster]{terra::SpatRaster} across time: annual totals, monthly cycle,
multi-year climatology, or SENAMHI hydrological seasons (Wet: Nov–Apr, Dry: May–Oct).
}
\examples{
\dontrun{
r <- pisco_read("monthly")
# Annual rainfall accumulation (mm/year)
r_ann <- pisco_aggregate(r, by = "year", fun = "sum")

# SENAMHI seasonal rainfall (Wet: Nov-Apr, Dry: May-Oct)
r_seas <- pisco_aggregate(r, by = "season_senamhi", fun = "sum")

# Multi-year mean monthly cycle
r_cycle <- pisco_aggregate(r, by = "month", fun = "mean")
}
}
