% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clip.R
\name{pisco_clip}
\alias{pisco_clip}
\title{Spatial Cropping and Masking for PISCO Rasters}
\usage{
pisco_clip(x, mask, crop_only = FALSE)
}
\arguments{
\item{x}{A \link[terra:SpatRaster]{terra::SpatRaster} object.}

\item{mask}{An \code{sf} object, \code{terra::SpatVector}, \code{sf::st_bbox}, \code{terra::SpatExtent},
or numeric bounding box (\code{c(xmin, ymin, xmax, ymax)}).}

\item{crop_only}{Logical. If \code{TRUE}, only crops to bounding box without masking values outside polygon.
Default is \code{FALSE}.}
}
\value{
A cropped (and optionally masked) \link[terra:SpatRaster]{terra::SpatRaster}.
}
\description{
Crops and masks a PISCO \link[terra:SpatRaster]{terra::SpatRaster} using a vector polygon (\code{sf} or \code{SpatVector}),
an \code{sf::st_bbox} object, or a spatial bounding box numeric vector.
}
\examples{
\dontrun{
r <- pisco_read("monthly")
# Crop by bounding box (e.g. Lima / Rimac basin)
r_sub <- pisco_clip(r, mask = c(-77.5, -12.5, -76.0, -11.5))

# Crop and mask using an sf polygon
# r_basin <- pisco_clip(r, mask = basin_sf)
}
}
